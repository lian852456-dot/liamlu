# 給 AI 協作者的指示（Codex / Claude / 小葳 / 小榮 / 其他）

本專案由多個 AI 助手與 Liam 共同維護。開工前請依序閱讀：

1. **`../AI協作中心/00_WEBSITE_INDEX.md`** 與 **`../AI協作中心/AI_WORKFLOW.md`** — 先確認本次是哪一個網站，以及正式版、測試版與禁止範圍。
2. 目標網站在 **`../AI協作中心/`** 內的正式 `PROJECT_HANDOFF.md`、`CURRENT_PROGRESS.md`、`KNOWN_ISSUES.md`、`TODO.md`、`CHANGELOG.md`。
3. **`CLAUDE.md`** — 專案架構、部署方式、踩過的坑（⚠️ 必讀，尤其是 GAS「存檔≠部署」與試算表欄位無聲丟失兩節）。內容對所有 AI 通用，不限 Claude。
4. **`docs/COLLAB-LOG.md`** — 跨 AI 協作日誌：各助手做過什麼、成功/失敗經驗、進行中事項。

## 協作規則

- **完成一項有意義的工作後**（新功能、修 bug、踩到新坑），在 `docs/COLLAB-LOG.md` 最上方追加一則紀錄，格式見該檔案開頭說明。
- **開工前先看日誌最近幾則**，避免重做別人做過的事、重踩已記錄的坑。
- 不重寫網站、不整檔覆蓋、不擅自修改資料流；只處理使用者明確指定的網站與檔案。
- 不確定的版本、狀態或素材先保留並標記「待確認」，不得自行刪除、回退或宣稱已完成。
- 靜態展示、占位資料、HTTP 200、本機測試、正式部署、正式資料驗證與 Liam／門市驗收是不同狀態，不得互相代替。
- 完成工作後同步更新正式交接文件與 `docs/COLLAB-LOG.md`；若交接文件不在本 repo，也必須在回報中明列是否已同步。
- 修改資料欄位時，務必照 `CLAUDE.md` 的「常用檢查清單」走完五步。
- GAS 相關改動（`gas/Code.gs`）本地無法測試（開發環境 proxy 封鎖 script.google.com），只能請 Liam 在瀏覽器開 `?action=debug` 等端點驗證。
- 密碼（GAS `PT_KEY`）只存在 GAS 編輯器裡，repo 只放 `CHANGE_ME` 佔位字，**不要 commit 真實密碼**。
  ※ 巡店曾在 2026-07-23～07-29 間免密碼（`ptAuthorized()` 直接 `return true`），
  但 **2026-07-29 起已改回真的檢查**（因為導覽首頁會給門市同仁用，Liam情報站的卡片會被看到，
  必須真的擋人）。改 Code.gs 時**不要**再把 `ptAuthorized()` 改回 `return true`。

## 🚨 兩條鐵則（違反過兩次，都造成線上事故）

**鐵則一：不要「整份檔案從自己的舊基準覆蓋」**
改 `index.html` / `patrol.html` / `gas/Code.gs` 這類共用大檔時，**先 pull 最新 main、只動自己那幾行**。
若從舊基準整檔輸出，會默默洗掉別人已合併的工作：
- 門市動物圖案被洗掉 **2 次**（2026-07-17、07-22）
- `gas/Code.gs` 的 `kpiCalc*` 自動化被洗掉，**同仁資料停更 4 天沒人發現**（2026-07-25）

**鐵則二：交給 Liam 貼進 GAS 的 `Code.gs`，必須是 repo main 最新版並自我驗證**
`CLAUDE.md` 的「🚨 2026-07-25 自動化被無聲洗掉事件」有完整檢查清單（函式清單 grep 指令）。
重點：**時間觸發器跑編輯器最新存檔的碼、不寄失敗信；同仁前台走已部署版本看起來卻正常** →
後台死掉會完全無聲。貼完務必確認 `setupKpiCalcWatchdog()` 是啟用狀態（唯一守門員）。

## 誰能做什麼（環境限制，避免互相誤會）

| 動作 | AI 助手 | Liam |
|---|---|---|
| 讀 Drive 日報、解析產生資料 JSON、改 repo、git 推送 | ✅ | ✅ |
| 呼叫 GAS 端點 | ❌（雲端 proxy 封鎖 script.google.com，實測 403） | ✅ |
| GAS 編輯器貼碼 / 執行函式 / 部署新版本 | ❌ **只有 Liam 能做** | ✅ |
| 寫入私有 Drive 資料夾（補 KPI 資料） | ✅ 可建檔，但**無刪檔權限** | ✅ |

→ 所以「請 AI 幫我在 GAS 執行」是做不到的，不要互相承諾；但 **AI 可直接寫私有 Drive
補 `north12b-kpicalc-<日期>.json` 救資料**（`kpiCalcAccess` 已改為讀最新那份）。

## 溝通管道

三方（Liam + 各 AI）沒有即時群組，以下列方式異步協作：

- 這份檔案 + `CLAUDE.md`：長期規則與知識。
- `docs/COLLAB-LOG.md`：工作交接與經驗分享。
- GitHub Issue / PR 留言：針對特定改動的討論。
