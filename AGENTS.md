# Agent guidelines

本文件是本 repo 唯一的 Agent 工作規則。產品行為以現行程式、測試與專案文件為準；日期型 handoff、incident、predeploy、QA 與 `docs/COLLAB-LOG.md` 只供查證，不構成永久限制。

## 工作邊界

- 先確認任務目標與受影響元件；只讀取完成任務所需的文件與程式。
- 可逆、範圍明確的修改可直接執行。只有需求會產生不同產品結果、需要新增權限，或涉及不可逆／高風險操作時才詢問。
- 未經 Liam 明確授權，不刪除正式資料、不變更存取權限、不提交秘密，也不執行 force-push、歷史重寫或無回復方案的 production 操作。
- 保留使用者及其他協作者的既有變更。修改共用大檔前先取得最新版本，採最小差異，避免以舊副本整檔覆蓋。

## 資料與安全

- GitHub Pages 只存公開介面與非敏感資產；員工資料與正式報表留在既有私有資料層。Repo 中的秘密只保留占位值，不得寫入真實 `PT_KEY`、管理者密碼或存取權杖。
- 正式資料來源、日期與 schema 以現有 contract 與程式驗證為準；來源不一致或資料不完整時 fail closed，不以舊快取或推測值冒充正式資料。
- 不任意更換既有資料流、驗證機制或 production backend。需要變更時，將其視為獨立範圍並驗證相依系統。

## 測試與發布

- 依變更風險選擇最小充分驗證：先跑受影響的單元／契約測試；共用資料契約、認證、發布流程或高影響模組再擴大回歸。只有 release gate 或廣泛變更才跑完整套件。
- 測試失敗時先判斷是否由本次變更造成；不得為了綠燈修改無關功能。回報新失敗與已知基準失敗的差異。
- 前端變更應檢查主要畫面與互動；production 發布前確認來源版本、範圍與可回復點，發布後驗證線上版本與關鍵讀取路徑。GAS 存檔不等於 Web App 部署；HTTP 200、本機成功或自動測試不能代替必要的視覺與正式資料驗證。
- 未完成必要驗證時，精確回報已完成、未驗證與受阻項目，不宣稱完整驗收。
- Liam 已授權既定每日戰報、台獎與網站資料流程在既有排程、收件人、權限及驗證通過後直接寄送／發布，不需逐次詢問；不得藉此擴張收件人、權限、資料範圍或 production 系統。

## 文件治理

- `README.md` 說明目前架構與操作入口；`CLAUDE.md` 保存現行產品契約與技術注意事項；`docs/` 保存專題契約與歷史證據。一般修改以 commit／PR 與測試結果為紀錄，只有新增長期知識或重要事故時才更新文件。
- 優先級統一為：目前任務明確要求與本文件 → 現行專案文件 → 歷史 evidence、handoff、incident、predeploy、QA 與協作日誌。發現衝突時採較高優先級來源，並移除或標記過期規則。
